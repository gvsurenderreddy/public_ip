## Customize the test machine
machine:
  # Version of ruby to use
  ruby:
    version:
      2.2.3

## Customize dependencies
dependencies:
  pre:
    - gem update --system
    - gem install bundler -v 1.10.6

test:
  override:
    - bundle exec rake
